#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

namespace ct {

    ??? find_first_multiple(...)
    {
	return find_if(begin , end, );
    }
}

int main () 
{
    using namespace ct;

    vector<int> vi{3, 5, 4, 12, 15, 7, 9};
    // int vi[]= {3, 5, 4, 12, 15, 7, 9};
    for (int i= 2; i < 10; ++i) {
	auto it= find_first_multiple(vi, i);
	if (  )
	    cout << "The first multiple of " << i << " is " << ??? << endl;
	else
	    cout << "There is no multiple of " << i << endl;
    }
        
    return 0;
}
  
 
